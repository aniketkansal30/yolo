services:
  - type: web
    name: yolo-damage-api-real
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT --workers 1 --threads 2 --timeout 180
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
